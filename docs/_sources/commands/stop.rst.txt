stop
==================

The `carme stop` command stop all applications indicated in the docker-compose.yaml file.

Usage
-----

Usage: carme stop [OPTIONS]

  Stop (and optionally remove, --remove) all containers running in
  background.

Options:
  --remove  Remove all images.
  --help    Show this message and exit.
