base-cpu
========

The base-cpu is based on the Docker Stacks Scipy image.  In order to ensure a consistent running environment running things other than Jupyter, everything but the start script for Jupyter is included.
